# myfilepath
> Hosted service where users can run the long running agent iterations. it will have a UI and a API.

- the UI will be a web app that allows users to run the agent iterations.
- the API will be a REST API that allows users to run the agent iterations.
- the agent iterations will be long running and will be able to run for a long time.

## Stories: 
  
  1) no auth, working shared sessions via link. usable, not customizable.
  - openai agents working in sandbox & alchemy environment
  - can we add in a "terminal viewer" so we can see / interact with the codex agents?
  
  2) reboot with remote.coey.dev stack (sveltekit full stack, has auth, d1, etc.)
  3) add pricing limits / tiers
import { describe, it } from 'vitest';

describe('003: Session page UI', () => {
  it.todo('GET /session/:sessionId returns HTML with tab bar + iframe region');
  it.todo('Each tab corresponds to /tab/:sessionId/:tabId (iframe src)');
  it.todo('Tab page connects to /terminal/:sessionId/:tabId/ws and is interactive');
});

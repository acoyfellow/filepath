import { describe, it } from 'vitest';

describe('WS-002: Start ttyd process in sandbox', () => {
  it.todo('POST /terminal/:id/start successfully starts ttyd');
  it.todo('sandbox.startProcess works');
  it.todo('waitForPort resolves without timeout');
});

name = "filepath-worker"
main = "src/index.ts"
compatibility_date = "2024-01-01"

[durable_objects]
bindings = [
  { name = "CONTAINER_MANAGER", class_name = "ContainerManager" }
]

[[migrations]]
tag = "v1"
new_classes = ["ContainerManager"]

[[migrations]]
tag = "v2"
deleted_classes = ["Sandbox", "TabStateDO", "SessionStateDO", "TabBroadcastDO"]

# Uncomment when CF Containers are configured
# [containers]
# bindings = [
#   { binding = "AGENT_CONTAINER", image = "filepath-agent" }
# ]
